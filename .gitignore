# Dependencies
node_modules/

# Environment variables files
# It's good practice to ignore all .env files to prevent accidental commits of sensitive data.
.env*
!.env.example # If you have an example env file, don't ignore it

# Runtime data and uploads
# Directory for files uploaded via the web interface
uploads/

# Directories for Command-Line Interface (CLI) processing
# These directories are typically for user-provided input or generated output
# and should not be version controlled.

data/

# Logs and other temporary files
*.log
npm-debug.log*
yarn-debug.log*
yarn-error.log*

# OS generated files
.DS_Store
Thumbs.db
._* # macOS resource fork files